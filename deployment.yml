apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone-devops
  labels:
    app: capstone-devops
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: capstone-devops
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: capstone-devops
    spec:
      containers:
        - image: 7akim/capstone-devops
          imagePullPolicy: Always
          name: capstone-devops
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: capstone-devops
  labels:
    app: capstone-devops
spec:
  type: LoadBalancer
  selector:
    app: capstone-devops
  ports:
    - port: 80
      targetPort: 80
      nodePort: 9090
      protocol: TCP